apiVersion: apps.gitlab.com/v1beta1
kind: Repository
metadata:
  name: example-repo
  namespace: gitlab
spec:
  project:
    name: my-example-repo
    description: "Example repository created via GitLab operator"
    visibility: public
    path: example-project
    namespace: gitlab
    initialize: true
    defaultBranch: main
    features:
      issues: true
      mergeRequests: true
      wiki: true
      snippets: true
  gitlab:
    instanceName: gitlab
